// Placeholder types — replace by running `npm run gen:types` once the schema
// in supabase/migrations has been applied to the linked Supabase project.
// (Requires SUPABASE_PROJECT_ID env var and `supabase` CLI login.)

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
